version: 2
jobs:
  build:
    docker:
      - image: constantinschreiber/rssi-polling:v1
    dependencies:
            - git clone git@github.com:codeuniversity/triangl-customer-service.git
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
